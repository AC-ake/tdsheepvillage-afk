# tdsheepvillage-afk 1.2

对比上一版（1.0）：新增「定时重开游戏（测试中）」「敲狼加速好友矿产」「运行日志（默认关闭）」，修掉了升塔和攻击抢鼠标的老问题，并完成 Python 3.13 及以上版本的适配。

## 🆕 新功能

- **定时重开游戏（测试中）**：新增「定时重开」标签页，可设时间（默认 00:01）。到点自动执行：点 `bwyc.png` 重开 → 等游戏加载 → 识别到 `choujiang.png` 就点 `jiangpin.png` → 等 5 秒后连点 `fanpai.png`（默认最多 3 次、每次间隔 1 秒）→ 等 1 秒点 `jinru.png` → 等 2 秒点 `qianxian.png`。点 `qianxian.png` 之前会先停掉正在跑的功能，之后再自动恢复**当时开启**的功能，原本关着的保持关闭。整段流程限制在设定的总时长内（默认 60 秒），某一步卡住会重新从 `bwyc.png` 开始，超时仍未完成就放弃本次重开并恢复功能；按 `F8` 可随时中止。
- **敲狼时加速好友矿产**：进入好友界面后先识别并点击 `jiasu.png`，等待 1 秒点确定，再继续原有的间谍狼流程。识别不到 `jiasu.png` 时会跳过加速、也不会盲点确定，直接进入间谍狼判定。
- **运行日志（默认关闭）**：每次运行按启动时间生成 `logs\年-月-日_时-分-秒.txt`。按 `F12`，或点启动页「停止全部 / F8」右边的「记录日志 F12」按钮开关。关闭时界面照常滚日志、只是不写文件；开关动作本身会记进文件方便回溯时间点。日志逐行实时写入，程序被强杀也不丢已写内容。
- **自动攻击增加无狼判断**：新增 `wulang.png` 识别，处于无狼状态时不再误点 `gongji.png`。
- **内置模板扩充**：`jiandielang.png`、`wulang.png`、`jiasu.png` 纳入内置模板，`templates` 文件夹自带同名图即可识别；「图像」文件夹里的同名图片仍然优先。

## 🔧 改进与修复

- **升塔与攻击不再抢鼠标**：两个功能同时开启时改为「先攻击一次 → 完整跑一轮升塔 → 再攻击」交替执行，一轮升塔期间不会插入攻击点击。
- **启动前检查顺序**：缺少塔坐标或缺少定时重开图片时先弹提示，不再把正在运行的功能一起停掉。
- **模板缺失判定放宽**：`templates` 或 `图像` 任一文件夹存在同名图片即视为不缺图。
- **界面版本信息**：窗口顶部单独一行显示版本号与作者信息，不与任何功能按钮重叠；窗口标题、脚本头部统一为 1.2。
- **配置向后兼容**：新增配置项在旧配置文件里缺失时自动用默认值，旧配置可直接继续使用；塔坐标仍然不保存。

## 🐍 Python 3.13 及以上版本适配

显式声明 `Pillow>=11.0.0`、`pyscreeze>=0.1.30`，并提升 `opencv-python` 至 `>=4.11.0.86`、`pynput` 至 `>=1.7.7`，避免 `pyautogui.screenshot()` 因依赖不兼容而失败。脚本使用 OpenCV `matchTemplate` 找图，不依赖 `pyscreeze.locateCenterOnScreen`，因此 pyscreeze 0.1.30 的异常行为变化不影响现有功能。

## 📦 下载与使用

1. 下载 `tdsheepvillage-afk1.2.zip` 并解压。
2. 保持 `tdsheepvillage-afk1.2.exe`、`templates`、`图像` 三个东西在**同一个文件夹**里，不要只单独拷 exe。
3. 双击 `tdsheepvillage-afk1.2.exe` 即可，无需再装 Python。
4. 游戏请使用 **1080P、100% 缩放**。
5. `F8` 全局急停，`F12` 开关运行日志，塔坐标不会保存（每次启动重新录入）。

压缩包内容：

```
tdsheepvillage-afk1.2.exe      主程序
templates\                     内置识图模板
图像\                          自定义识图图片（同名优先于内置模板）
README.md / 更新日志.md / 发行说明-1.2.md
```

## ⚠️ 说明

- 定时重开仍处于测试阶段，建议先观察一两次运行日志再长期挂机。
- 这是 PyInstaller 打包的单文件程序，个别杀毒软件可能误报，加信任即可。
- 运行日志默认关闭，需要排查问题时按 F12 打开，日志写在 exe 同级的 `logs` 文件夹里。
