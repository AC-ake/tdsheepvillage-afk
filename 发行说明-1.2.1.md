# tdsheepvillage-afk 1.2.1

对比 1.2：修复定时重开在同一分钟内重复触发导致的无限重开，修复敲狼加速到上限时卡在确定框，以及「点确定后等待」配置项不生效的问题。

## 🐞 修复

- **定时重开不再重复触发（重要）**
  1.2 里“已经触发过”的标记记在调度线程内部，而重开流程结束后会重启这个线程。如果流程在 00:01 末尾才跑完，
  新线程会再次判定“到点”并立刻开始第二轮重开；此时游戏已跳过签到抽奖界面，图片识别不到就会被当成流程没走完，
  于是无限重开。现在标记改为记录在程序内部（按“日期 + 目标分钟”），同一分钟只触发一次，下一次要等第二天同一时间。
- **单次重开总时长下限 61 秒**
  设定值小于 61 秒时自动按 61 秒执行，并在日志里说明。正常流程走完还要等抽奖、翻牌动画，时间过短容易在同一分钟内被再次触发。
- **加速到上限不再卡流程**
  加速到第 5 次会连续弹出「加速奖励」和「加速已到上限」两个确定框，1.2 只点一次确定，剩下的框会把敲狼流程卡住。
  现在按「加速后确定次数上限」（默认 2）连点确定，次数可在敲狼页调整。
- **「点确定后等待」正式生效**
  这个配置项在 1.2 里只出现在界面上，代码从未读取。现在敲狼点完确定会按它等待，再去点好友右箭头。

## 📦 下载与使用

1. 下载 `tdsheepvillage-afk1.2.1.zip` 并解压。
2. 保持 exe、`templates`、`图像` 三者同一个文件夹，不要只单独拷 exe。
3. 双击 `tdsheepvillage-afk1.2.1.exe`，无需安装 Python。
4. 游戏 1080P、100% 缩放；`F8` 全局急停，`F12` 开关运行日志。

## ⚠️ 说明

- 从 1.2 升级可以直接沿用原配置文件，新增字段会自动使用默认值。之前把「单次重开总时长」设成 60 的，运行时会按 61 秒执行（日志会说明），
  想让界面直接显示 61，可以在定时重开页改成 61 再点“保存配置”。
- 定时重开仍处于测试阶段，建议先看一两次运行日志再长期挂机。
