# 更新日志

## 1.2.1（2026-09-20）

### 修复

- **定时重开不再重复触发（重要）**
  1.2 中“已经触发过”的标记记在调度线程内部，而重开流程结束后会重启这个线程。流程如果在 00:01 末尾才跑完，
  新线程会再次判定成“到点”并立刻开始第二轮重开；此时游戏已经跳过签到抽奖界面，图片识别不到就会被当成流程没走完，
  于是陷入无限重开。现在标记改为记录在程序内部（按“日期 + 目标分钟”），同一分钟内只会触发一次，下一次要等到第二天同一时间。
- **单次重开总时长下限 61 秒**
  设定值小于 61 秒时自动按 61 秒执行并在日志中说明。正常流程走完还要等抽奖、翻牌动画，时间过短容易在同一分钟内被再次触发。
- **敲狼加速到上限不再卡流程**
  加速到第 5 次会连续弹出「加速奖励」和「加速已到上限」两个确定框，1.2 只点一次确定，剩下的框会把流程卡住。
  现在按「加速后确定次数上限」（默认 2）连点确定，次数可在敲狼页调整。
- **「点确定后等待」正式生效**
  该配置项在 1.2 里只出现在设置界面，代码从未读取。现在敲狼点完确定会按它等待，再点好友右箭头。

## 1.2（2026-09-19）

### 新加入

- **定时重开游戏（测试中）**
  新增「定时重开」标签页，可单独勾选启用。到设定时间（默认 `00:01`，24 小时制，可在界面修改）后自动执行：
  点击 `bwyc.png` 重开 → 等待 → 识别到 `choujiang.png` 就点 `jiangpin.png` → 等待后连点 `fanpai.png`（默认最多 3 次、每次间隔 1 秒）
  → 等待后点 `jinru.png` → 等待后点 `qianxian.png`。
  点 `qianxian.png` 之前会先停掉正在运行的功能，之后再自动恢复**当时开启**的功能，原本关闭的功能保持关闭。
  整段流程限制在设定总时长内（默认 60 秒），某一步卡住会重新从 `bwyc.png` 开始；总时长内仍未完成则放弃本次重开并恢复功能。
  按 `F8` 可在任意时刻中止，中止后不会自动恢复功能。
- **敲狼时加速好友矿产**
  进入好友界面后先识别并点击 `jiasu.png`，等待后点 `queding.png`，再继续原有的间谍狼识别流程。
  识别不到 `jiasu.png` 时跳过加速，也不会点确定，直接进入间谍狼判定；原本「未识别到 `jiandielang.png` 就跳过确定、直接翻下一位好友」的规则保持不变。
- **运行日志（默认关闭）**
  每次运行按启动时间生成 `logs\年-月-日_时-分-秒.txt`（24 小时制、精确到秒）。按 `F12`，或点启动页「停止全部 / F8」右侧的「记录日志 F12」按钮开关。
  关闭记录时界面照常显示日志，只是不写文件；开关动作本身会写进文件，方便回看开启/关闭的时间点。日志逐行实时写入，程序被强制结束也不会丢已写内容。
- **窗口内版本信息**
  窗口最上方单独一行显示「羊村挂机助手1.2」与作者信息，不再与其他功能按钮或日志框重叠。
- **自动攻击增加无狼判断**
  新增 `wulang.png` 识别：处于无狼状态时不再误点 `gongji.png`，直接按「未发现可攻击目标」处理并等待。
- **内置模板扩充**
  `jiandielang.png`、`wulang.png`、`jiasu.png` 纳入内置模板名单：`templates` 文件夹内自带同名图片即可识别，也一并纳入打包 exe 时的模板抽取范围；「图像」文件夹里的同名图片仍然优先。

### 修改

- **升塔与攻击不再抢鼠标**：两个功能同时开启时改为「先攻击一次 → 完整执行一轮升塔 → 再攻击」交替执行，一轮升塔期间不会插入攻击点击。
- **启动前的检查顺序调整**：缺少塔坐标或缺少定时重开图片时先弹提示，不再把正在运行的功能一起停掉。
- **模板缺失判定放宽**：`templates` 或 `图像` 任一文件夹存在同名图片即视为不缺图，避免只用自定义图片时误报「缺少识图模板」。
- **界面与版本号**：窗口标题、脚本头部版权说明统一为 1.2；启动页作者信息与窗口顶部保持一致。
- **README 补充图片命名**：新增 `wulang.png`、`jiasu.png` 的说明。
- **配置文件保持向后兼容**：新增的配置字段在旧配置文件里缺失时自动使用默认值，旧配置可以直接继续用；塔坐标仍然不保存。

### 适配 Python 3.13 及更高版本

显式声明 `Pillow>=11.0.0`、`pyscreeze>=0.1.30`，并提升 `opencv-python` 至 `>=4.11.0.86`、`pynput` 至 `>=1.7.7`，避免 `pyautogui.screenshot()` 因依赖不兼容而失败。脚本使用 OpenCV `matchTemplate` 找图，不依赖 `pyscreeze.locateCenterOnScreen`，因此 pyscreeze 0.1.30 的异常行为变化不影响现有功能。

### 测试状态

- 已经实测通过：敲狼加速好友矿产、运行日志写入与开关、升塔与攻击的先后次序、无狼判断。
- 仍在测试：定时重开游戏。`fanpai.png`识别效果还需要签到满 3 天后再验证。
